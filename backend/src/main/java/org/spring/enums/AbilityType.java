package org.spring.enums;

public enum AbilityType {
    COMBAT,     // {Gain X Combat}
    TRADE,       // {Gain X Trade}
    DRAW,       // Draw X cards
    SCRAP,      // {Scrap}: ...
    DESTROY,    // {Destroy}: target base
    DISCARD,
    ACQUIRE,
    ALLY,
    AUTHORITY,
    COPY,
    OTHER       // для всех остальных спецэффектов
}
